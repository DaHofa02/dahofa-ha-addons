# Checkmate – MongoDB

This add-on provides the MongoDB database required by the **Checkmate – Uptime Monitor** add-on.

## Setup

1. Install and start this add-on first
2. Then install and start the **Checkmate – Uptime Monitor** add-on
3. In the Checkmate add-on config, set `mongodb_host` to the IP of your Home Assistant instance
